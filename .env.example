# Local development values only. Copy to .env before later infrastructure phases.
PROJECT_NAME=cloud-resource-autoscaling
PROJECT_LABEL=com.ml-autoscaler.project=cloud-resource-autoscaling
APPLICATION_ROLE_LABEL=com.ml-autoscaler.role=application
MANAGED_LABEL=com.ml-autoscaler.managed=true

# Planned service endpoints; unused until their respective implementation phases.
MONGODB_URI=mongodb://mongodb:27017
MONGODB_DATABASE=cloud_autoscaling
NGINX_CONTROL_CONTAINER=nginx
DOCKER_HOST=
